import React from 'react';

interface Props {
    className?: string;
}

export const TitleRightSide: React.FC<Props> = ({ className }) => {
  return (
    <div className={className}>
        
    </div>
  );
};